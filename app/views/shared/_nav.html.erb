<nav>

  <% if user_signed_in? %>
    <p class="nav user">Welcome, <b><%= current_user.email %></b> Guru</p>
  <% end %>

  <h2 class="nav nav-title"> <%= link_to 'TestGuru Project', root_path %></h2>

  <ul class="nav-list">
    <% unless user_signed_in? %>
      <li class="nav-item"><%= link_to 'Register', new_user_registration_path, class:"nav nav-link" %></li>
      <li class="nav-item"><%= link_to 'Login', new_user_session_path, class:"nav nav-link" %></li>
    <% end %>
    <% if user_signed_in? %>
      <li class="nav-item"><%= button_to 'Logout', destroy_user_session_path, class:"nav nav-link", 
                                                                              method: :delete,
                                                                              data: {confirm: "Are you sure"} %></li>
    <% end %>
  </ul>

</nav>

