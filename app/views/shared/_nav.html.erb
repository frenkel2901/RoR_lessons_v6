<nav>

  <% if logged_in? %>
    <p class="nav user">Welcome, <b><%= current_user.name %></b> Guru</p>
  <% end %>

  <h2 class="nav nav-title"> <%= link_to 'TestGuru Project', root_path %></h2>
  <ul class="nav-list">
    <% unless logged_in? %>
    <li class="nav-item"><%= link_to 'Register', signup_path, class:"nav nav-link" %></li>
    <li class="nav-item"><%= link_to 'Login', login_path, class:"nav nav-link" %></li>
    <% end %>
    <% if logged_in? %>
      <li class="nav-item"><%= link_to 'Logout', logout_path, class:"nav nav-link" %></li>
    <% end %>
  </ul>

</nav>

