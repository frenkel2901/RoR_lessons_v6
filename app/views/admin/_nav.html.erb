<nav>
  <div class="nav nav-admin">
    <span class="nav-admin title"><%= t('nav.admin_header') %></span>

    <div class="nav-admin user">
      <%= t('nav.welcome', user_email: current_user.email) %>
      <%= link_to t('nav.logout'), destroy_user_session_path, method: :delete  %>
    </div>
  
  </div>

  <% if current_user.is_a?(Admin) %>
    <%= link_to t('nav.user_view'), root_path %>                                    
  <% end %>

</nav>
