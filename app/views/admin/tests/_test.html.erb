<tr>
  <td><%= link_to test.title, admin_test_path(test)%></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('action.edit'), edit_admin_test_path(test) %></td>
  <td><%= button_to t('action.delete'), admin_test_path(test),
                              method: :delete,
                              data: { confirm: t('action.confirm') } %></td>
</tr>
