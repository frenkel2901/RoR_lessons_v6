<div class="container">
  <h3><%= t('.header') %></h3>
  <span>
    <%= link_to t('.new'), new_admin_badge_path %>
  </span>
  <table class="table align-center">
    <tr>
      <th>
        <%= t('label.title') %>
      </th>
      <th><%= t('.image') %></th>
      <th><%= t('label.rule_name') %></th>
      <th><%= t('label.rule_value') %></th>
      <th colspan="3"><%= t('label.actions') %></th>
    </tr>
    <%= render @badges %>
  </table>
</div>
