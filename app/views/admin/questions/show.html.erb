<p><b>Question:</b> <%= @question.body %></p>
<p>
<%= link_to 'New Answer', new_admin_question_answer_path(@question)  %>
<ol>
  <%= render @question.answers %>
</ol>
<%= link_to 'Edit', edit_admin_question_path(@question) %> |
<%= link_to 'Back', admin_test_path(@question.test_id)  %></p>
<%= button_to 'Delete', @question, method: :delete, data: { confirm: 'Are you sure' } %>
