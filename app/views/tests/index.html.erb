<h1><%= t('nav.tests') %></h1>

<table>
  <tr>
    <th><%= t('label.title') %></th>
    <th><%= t('label.level') %></th>
    <th><%= t('label.questions') %></th>
    <th><%= t('label.start') %></th>
  </tr>
  <%= render @tests %>
</table>
