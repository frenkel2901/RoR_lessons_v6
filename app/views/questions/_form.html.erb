<%= form_with model: [@test, @question], local: true do |form| %>
<%= render 'shared/errors', resourse: @question %>
<p><%= question_header(@question) %></p>

  <p>
    <%= form.label :body  %> <br>
    <%= form.text_field :body %>
  </p>
  
  <p><%= form.submit %></p>
<% end %>
