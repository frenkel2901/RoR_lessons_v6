<p><b>Question:</b> <%= @question.body %></p>
<p>
  <%= link_to 'Edit', edit_question_path(@question)  %> |
  <%= link_to 'Delete',
                question_path(@question),
                method: :delete,
                data: { confirm: 'Are you sure?'} %></p>
                
<p><%= link_to 'Back', test_path(@question.test_id)  %></p>